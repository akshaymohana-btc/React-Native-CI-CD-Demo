pipeline {
    agent any

    stages {
        stage('Git Checkout') {
            steps {
                git url: 'git@github.com:akshaymohana-btc/React-Native-CI-CD.git'
            }
        }
    }
}
